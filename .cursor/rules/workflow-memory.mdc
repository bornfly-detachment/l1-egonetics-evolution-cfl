---
description: 对话工作流与 workflow_state 记忆（中文）
alwaysApply: true
---

# 工作流记忆

## 对话开始（必做）

1. 读取 **egonetics-evolution 根目录**（非 opencode）：
   - `project_config.md` — 稳定路径、职责、验证命令
   - `workflow_state.md` — 当前任务、焦点、最近决策
2. 若用户任务明确属于 **仅前端**，再打开 opencode 中相关文件；仍遵守 `general.mdc` 边界。

## 执行中

- 跨仓改动：先更新 `workflow_state.md` 的 `active_task` / `notes`，再动手。
- 大改动前列出假设与验证命令（见 `project_config.md`）。

## 对话结束前（必做）

更新 `workflow_state.md`：

- `active_task`：完成摘要或下一步
- `last_updated`：ISO 日期
- `recent_decisions`：追加 1–3 条要点（可滚动删除过旧条目）
- `workspace_focus`：保持 `evolution+opencode` 除非用户改范围

不要编造未执行的验证结果；未跑过的命令标为「待验证」。
